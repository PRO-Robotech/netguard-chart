{{- $backend  := .Values.backend -}}
{{- $sa       := $backend.serviceAccount -}}
{{- $saName   := default (printf "%s-backend" (include "netguard-chart.fullname" .)) $sa.name -}}

{{- if and $backend.enabled $sa.create -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $saName }}
  labels:
    app.kubernetes.io/component: backend
    {{- include "netguard-chart.labels" . | nindent 4 }}
  {{- with $sa.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ $sa.automount }}
{{- end }}
